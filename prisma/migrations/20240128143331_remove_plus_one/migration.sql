/*
  Warnings:

  - You are about to drop the column `allowed_plus_one` on the `plus_one` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE "plus_one" DROP COLUMN "allowed_plus_one";
